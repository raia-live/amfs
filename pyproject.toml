[project]
name = "amfs-workspace"
version = "0.0.0"
description = "AMFS workspace root"
requires-python = ">=3.11"
license = "Apache-2.0"

[dependency-groups]
dev = [
    "pytest>=8.0",
    "pytest-asyncio>=0.23",
    "amfs-core",
    "amfs-adapter-filesystem",
    "amfs",
    "amfs-cli",
    "amfs-mcp-server",
    "amfs-crewai",
    "amfs-langchain",
    "amfs-langgraph",
    "amfs-autogen",
    "amfs-strands",
    "amfs-sprites",
]

[tool.uv.workspace]
members = [
    "packages/core",
    "packages/adapters/filesystem",
    "packages/adapters/postgres",
    "packages/adapters/s3",
    "packages/adapters/http",
    "packages/sdk-python",
    "packages/cli",
    "packages/mcp-server",
    "packages/http-server",
    "packages/connectors",
    "packages/patterns",
    "packages/cortex",
    "packages/integrations/crewai",
    "packages/integrations/langchain",
    "packages/integrations/langgraph",
    "packages/integrations/autogen",
    "packages/integrations/strands",
    "packages/integrations/sprites",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"

[tool.ruff]
target-version = "py311"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I", "N", "W", "UP"]

[tool.uv.sources]
amfs-core = { workspace = true }
amfs-adapter-filesystem = { workspace = true }
amfs-adapter-postgres = { workspace = true }
amfs-adapter-s3 = { workspace = true }
amfs-adapter-http = { workspace = true }
amfs = { workspace = true }
amfs-cli = { workspace = true }
amfs-mcp-server = { workspace = true }
amfs-http-server = { workspace = true }
amfs-connectors = { workspace = true }
amfs-patterns = { workspace = true }
amfs-cortex = { workspace = true }
amfs-crewai = { workspace = true }
amfs-langchain = { workspace = true }
amfs-langgraph = { workspace = true }
amfs-autogen = { workspace = true }
amfs-strands = { workspace = true }
amfs-sprites = { workspace = true }

[tool.mypy]
python_version = "3.11"
strict = true
